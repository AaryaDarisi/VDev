<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="navbar">
        <!-- <img src="VITLogoEmblem.png" alt="logo" class="logo"> -->
    </div>
    <div class="headtxt">
        <h1 class="title">Student id: <%=ans.id%></h1>
    </div>
    <div class="ques">
        <p class="question">Q. <%=question%></p>
    </div>
    <form action="/submit" method="post" id="core">
        <textarea class="txt" name="code" id="code" cols="50" rows="50" placeholder="//your HTML goes here" readonly onkeydown="if(event.keyCode===9){var v=this.value,s=this.selectionStart,e=this.selectionEnd;this.value=v.substring(0, s)+'\t'+v.substring(e);this.selectionStart=this.selectionEnd=s+1;return false;}"><%=ans.code%></textarea>
        <textarea class="css txt" name="code2" id="code" cols="50" rows="50" placeholder="//your CSS goes here" readonly onkeydown="if(event.keyCode===9){var v=this.value,s=this.selectionStart,e=this.selectionEnd;this.value=v.substring(0, s)+'\t'+v.substring(e);this.selectionStart=this.selectionEnd=s+1;return false;}"><%=ans.code2%></textarea>
        <textarea class="js txt" name="code3" id="code" cols="50" rows="50" placeholder="//your JS goes here" readonly onkeydown="if(event.keyCode===9){var v=this.value,s=this.selectionStart,e=this.selectionEnd;this.value=v.substring(0, s)+'\t'+v.substring(e);this.selectionStart=this.selectionEnd=s+1;return false;}"><%=ans.code3%></textarea>
    </form>
    <br>
    <button onclick="hi()">Output</button>
    <div class="inactive">
        <p class="output">Output</p>
        <iframe class="result"></iframe>
    </div>
    <div class="score">
        <form action="/submit/<%=ans.id%>?_method=PATCH" method="post" id="mark">
            <input type="text" name="marks" id="marks" value="<%=ans.score%>">
            <button class="post">Post</button>
        </form>
    </div>
    <script src="/view.js"></script>
</body>
</html>